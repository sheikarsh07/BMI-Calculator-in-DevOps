apiVersion: v1
kind: Service
metadata:
  name: bmi-calculator-svc
spec:
  type: NodePort
  selector:
    app: bmi-calculator
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080   # access app at http://<node-ip>:30080
